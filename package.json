{
  "name": "notion_webdump",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/index.ts --bundle --outdir=build --platform=node --watch --servedir=build",
    "watch": "esbuild src/index.ts --bundle --outdir=build --platform=node --watch",
    "build": "esbuild src/index.ts --bundle --outdir=build --platform=node",
    "serve": "node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.5",
    "@types/node-persist": "^3.1.3",
    "esbuild": "^0.17.12",
    "prettier": "^2.8.6"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.3",
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "node-persist": "^3.1.3",
    "telegraf": "^4.12.2"
  }
}